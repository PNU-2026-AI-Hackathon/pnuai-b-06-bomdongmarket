# 이 파일을 .env로 복사한 뒤 placeholder를 로컬 비밀값으로 교체하세요.
# .env는 .gitignore에 등록되어 커밋되지 않습니다.

# MySQL 컨테이너용 로컬 비밀번호
DB_PASSWORD=replace-with-local-db-password

# JWT 서명 키(최소 32바이트)
JWT_SECRET=replace-with-at-least-32-character-secret

# JWT 만료 시간(ms, 기본 1일)
JWT_EXPIRATION=86400000
